import { NativeBaseProvider, Text } from "native-base";
import React from "react";

const Banner = () => {
  return (
    <NativeBaseProvider>
      <Text>tes</Text>
    </NativeBaseProvider>
  );
};

export default Banner;
